. $(dirname $0)/monobin-env

MONO_PREFIX=$(dirname $0)/..
MONO=$MONO_PREFIX/$MONOBINDIR/mono

export MONO_PATH=$MONO_PREFIX/lib/mono/2.0
export MONO_CFG_DIR=$MONO_PREFIX/etc
export LD_LIBRARY_PATH=$MONO_PREFIX/lib
