thisdir = mbas/Test
SUBDIRS = misc dlls tests errors

DISTFILES = test-mbas.make test-mbas.pl README.tests

include ../../build/rules.make

all-local $(STD_TARGETS:=-local):
	@:

dist-local: dist-default
