diff --git a/mono/config.make b/mono/config.make
index 00bd4a091..1cd5b7e8b 100644
--- a/mono/config.make
+++ b/mono/config.make
@@ -4,16 +4,22 @@ DEFAULT: all
 
 monocmd = $(shell which mono)
 monocmddir = $(dir $(monocmd))
-prefix = $(shell (cd $(monocmddir)/..; pwd))
+prefix = $(shell (cd $(monocmddir)/..; pwd))/
 thisdir = $(dir $(realpath $(lastword $(MAKEFILE_LIST))))
 topdir = $(thisdir)../
 builddir = $(topdir)
-libdir = $(prefix)/lib/
-bindir = $(prefix)/bin/
+libdir = lib/
+bindir = bin/
 monobindir = $(bindir)
 monolibdir = $(libdir)
 monodir = $(monolibdir)mono
 
+ifndef DESTDIR
+override DESTDIR := $(prefix)/
+else
+override DESTDIR := $(DESTDIR)/
+endif
+
 debugvars:
 	@echo prefix=$(prefix)
 	@echo topdir=$(topdir)
@@ -144,10 +150,11 @@ NO_DIST = .gitignore lib/debug lib/proto lib/release
 #
 install-sdk-lib:
 	@echo "Installing $(ASSEMBLY)"
-	@mkdir -p $(DESTDIR)$(monodir)/fsharp
+	-ls -ld $(DESTDIR)$(monodir)
+	mkdir -vp $(DESTDIR)$(monodir)/fsharp
 	@if test "x$(DELAY_SIGN)" = "x1"; then \
 	    echo "Signing $(outdir)$(ASSEMBLY) with Mono key"; \
-	    $(monobindir)sn -q -R $(outdir)$(ASSEMBLY) $(topdir)mono/mono.snk; \
+	    $(prefix)$(monobindir)sn -q -R $(outdir)$(ASSEMBLY) $(topdir)mono/mono.snk; \
 	fi
 	@if test x-$(NAME) = x-FSharp.Compiler.Private; then \
 	    echo "Installing extra dependency System.Collections.Immutable.dll to $(DESTDIR)$(monodir)/fsharp/"; \
